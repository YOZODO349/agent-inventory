[project]
name = "agent-asset-overview"
version = "0.6.0"
description = "把本机散落的 AI Agent 客户端 / Skill / MCP 服务 / 插件 / 工具，扫成一张可点的名册"
readme = "README.md"
requires-python = ">=3.9"
license = { file = "LICENSE" }
keywords = ["agent", "mcp", "skill", "launcher", "inventory", "tkinter", "windows"]
dependencies = []

[dependency-groups]
dev = ["pyinstaller>=6.0"]

[tool.uv]
# 这是一个脚本式项目，不需要作为包安装进环境
package = false
